{
  "version": 2,
  "regions": ["sin1"],
  "buildCommand": "cd frontend && npm install && npm run build",
  "functions": {
    "api/index.js": { "memory": 256 }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.js" }
  ]
}
