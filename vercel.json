{
  "version": 2,
  "regions": ["sin1"],
  "buildCommand": "cd frontend && npm install && npm run build",
  "functions": {
    "api/index.js": { "memory": 256, "includeFiles": "frontend/dist/**" }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.js" }
  ]
}
